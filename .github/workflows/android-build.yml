
name: Build Android APK (Elite Production)

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Generate Production APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Build Next.js Web App (Static Export)
        run: npm run build

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Capacitor Sync
        run: |
          npx cap add android || true
          npx cap sync android

      - name: 🔑 EXTRACT SHA-256 FINGERPRINT (Copy this for Firebase)
        run: |
          cd android
          chmod +x gradlew
          ./gradlew signingReport > report.txt
          echo "------------------------------------------------------------"
          echo "🚀 YOUR SHA-256 FINGERPRINT FOR FIREBASE (ANDROID APP):"
          grep -A 3 "Variant: debug" report.txt | grep "SHA-256" || true
          echo "------------------------------------------------------------"

      - name: Build APK
        run: |
          cd android
          ./gradlew assembleDebug

      - name: Upload Finished APK
        uses: actions/upload-artifact@v4
        with:
          name: VideoMasterAI-Final-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 7
