/**
 * 🛠️ MIDDLEWARE REMOVED: Incompatible with 'output: export'.
 * All route protection is now handled in Client-Side AuthGuards.
 */
export default function middleware() {
  return;
}

export const config = {
  matcher: [],
};
